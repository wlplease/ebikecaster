// ══════════════════════════════════════
// REMOVED RULES — saved for other apps
// These were stripped from Farbits firestore.rules
// since each app now has its own Firebase rules.
// ══════════════════════════════════════

// ── FARPOOLS (names, pool-activity, poolSettings, pool-chat, pool-rivalries, pool-seasons, farpools-*) ──
// NOTE: "pools" collection was KEPT — it's used by Farbits farpool-tab

match /names/{nameId} {
  allow read: if true;
  allow create: if isValidString(request.resource.data.name, 1, 100)
                && isValidAddress(request.resource.data.ownerAddress)
                && request.resource.data.burnAmount is int
                && request.resource.data.burnAmount >= 0
                && request.resource.data.size() <= 50000;
  allow update: if isValidString(request.resource.data.name, 1, 100)
                && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /name-history/{entryId} {
  allow read: if true;
  allow create: if isValidString(request.resource.data.name, 1, 100)
                && isValidString(request.resource.data.type, 1, 50)
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /pool-activity/{activityId} {
  allow read: if true;
  allow create: if request.resource.data.poolId is string
                && request.resource.data.poolId.size() > 0
                && request.resource.data.poolId.size() <= 100
                && isValidString(request.resource.data.type, 1, 50)
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /poolSettings/{poolId} {
  allow read: if true;
  allow create, update: if request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /pool-chat/{messageId} {
  allow read: if true;
  allow create: if request.resource.data.poolId is string
                && request.resource.data.poolId.size() > 0
                && request.resource.data.fid is int
                && request.resource.data.fid > 0
                && isValidString(request.resource.data.text, 1, 500)
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /pool-rivalries/{rivalryId} {
  allow read: if true;
  allow create: if request.resource.data.challengerPoolId is string
                && request.resource.data.defenderPoolId is string
                && isValidString(request.resource.data.metric, 1, 50)
                && request.resource.data.size() <= 50000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['status', 'result', 'completedAt'])
                && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /pool-seasons/{seasonId} {
  allow read: if true;
  allow write: if false;
}

match /farpools-visitors/{visitorId} {
  allow read: if false;
  allow write: if false;
}

match /farpools-announcements/{announcementId} {
  allow read: if true;
  allow write: if false;
}

match /farpools-config/{docId} {
  allow read: if true;
  allow write: if false;
}


// ── FARFAUCET / FARISLAND ──

match /faucet-visitors/{visitorId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /faucet-announcements/{announcementId} {
  allow read: if true;
  allow write: if false;
}

match /faucet-claims/{claimId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && isValidUsername(request.resource.data.username)
                && isValidAddress(request.resource.data.address)
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /faucet-fids/{fidId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.size() <= 10000;
  allow delete: if false;
}

match /faucet-donations/{donationId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && isValidUsername(request.resource.data.username)
                && isValidAddress(request.resource.data.address)
                && request.resource.data.amount is int
                && request.resource.data.amount > 0
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /faucet-blocked/{attemptId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.size() <= 10000;
  allow delete: if false;
}

match /hodl-mining/{scoreId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && isValidUsername(request.resource.data.username)
                && request.resource.data.weekKey is string
                && request.resource.data.weekKey.size() <= 20
                && request.resource.data.size() <= 10000;
  allow update: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && request.resource.data.size() <= 10000;
  allow delete: if false;
}

match /faucet-song-votes/{voteId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.songIndex is int
                        && isValidString(request.resource.data.songName, 1, 200)
                        && request.resource.data.size() <= 10000;
  allow delete: if false;
}

match /faucet-guestbook/{entryId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /faucet-riddle-guesses/{guessId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /faucet-riddle-keys/{keyId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.size() <= 10000;
  allow delete: if false;
}

match /faucet-artcart-visits/{visitId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /faucet-chat-flags/{flagId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /faucet-referrals/{referralId} {
  allow read: if true;
  allow create: if request.resource.data.referrerFid is int
                && request.resource.data.referrerFid > 0
                && request.resource.data.referredFid is int
                && request.resource.data.referredFid > 0
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}


// ── FARHEIST / ISLAND EXTRAS ──

match /dune-cache/{cacheId} {
  allow read: if true;
  allow write: if false;
}

match /glaze-daily/{dayId} {
  allow read: if true;
  allow write: if false;
}

match /price_reference/{refId} {
  allow read: if true;
  allow write: if false;
}

match /notification_tokens/{tokenId} {
  allow read: if false;
  allow write: if false;
}

match /schedule-overrides/{overrideId} {
  allow read: if true;
  allow write: if false;
}

match /crew-mine-settings/{settingId} {
  allow read: if true;
  allow write: if false;
}

match /crew-mine-stats/{statId} {
  allow read: if true;
  allow create, update: if request.resource.data.fid is int
                        && request.resource.data.fid > 0
                        && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /sprinkles-stats-users/{userId} {
  allow read: if true;
  allow create, update: if request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /farcots-challenges/{challengeId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.challenger)
                && request.resource.data.size() <= 50000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['status', 'winner', 'completedAt', 'opponent'])
                && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /farcots-progress/{progressId} {
  allow read: if true;
  allow create, update: if isValidAddress(request.resource.data.address)
                        && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /stats/{statId} {
  allow read: if true;
  allow write: if false;
}


// ── FARPIZZA ──

match /farpizza-bounties/{bountyId} {
  allow read: if true;
  allow create: if request.resource.data.bountyId is int
                && request.resource.data.bountyId >= 0
                && isValidAddress(request.resource.data.poster)
                && isValidString(request.resource.data.status, 1, 50)
                && request.resource.data.size() <= 100000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['status', 'submissionCount', 'winner', 'payout', 'burned', 'completedAt',
                             'cancelledAt', 'reclaimedAt'])
                && request.resource.data.size() <= 100000;
  allow delete: if false;
}

match /farpizza-users/{userId} {
  allow read: if true;
  allow create, update: if isValidAddress(request.resource.data.address)
                        && request.resource.data.size() <= 100000;
  allow delete: if false;
}

match /farpizza-activity/{activityId} {
  allow read: if true;
  allow create: if isValidString(request.resource.data.type, 1, 50)
                && request.resource.data.bountyId is int
                && request.resource.data.bountyId >= 0
                && isValidAddress(request.resource.data.actor)
                && request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /farpizza-votes/{voteId} {
  allow read: if true;
  allow create: if request.resource.data.bountyId is int
                && request.resource.data.bountyId >= 0
                && isValidAddress(request.resource.data.voter)
                && request.resource.data.submissionIndex is int
                && request.resource.data.submissionIndex >= 0
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /farpizza-listings/{listingId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.seller)
                && isValidString(request.resource.data.title, 1, 200)
                && request.resource.data.price is number
                && request.resource.data.price >= 0
                && request.resource.data.size() <= 100000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['status'])
                && request.resource.data.size() <= 100000;
  allow delete: if true;
}

match /farpizza-visitors/{visitorId} {
  allow read: if false;
  allow write: if false;
}

match /farpizza-announcements/{announcementId} {
  allow read: if true;
  allow write: if false;
}

match /farpizza-config/{docId} {
  allow read: if true;
  allow write: if false;
}

match /farpizza-notifications/{notificationId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.recipientAddress)
                && isValidString(request.resource.data.title, 1, 200)
                && isValidString(request.resource.data.type, 1, 50)
                && request.resource.data.size() <= 50000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['read'])
                && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /farpizza-bookmarks/{bookmarkId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.address)
                && request.resource.data.bountyId is int
                && request.resource.data.bountyId >= 0
                && request.resource.data.size() <= 10000;
  allow update: if false;
  allow delete: if true;
}

match /farpizza-bounty-categories/{categoryId} {
  allow read: if true;
  allow create: if request.resource.data.bountyId is int
                && request.resource.data.bountyId >= 0
                && isValidString(request.resource.data.category, 1, 100)
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /farpizza-achievements/{achievementId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.address)
                && isValidString(request.resource.data.achievementId, 1, 100)
                && request.resource.data.size() <= 50000;
  allow update: if request.resource.data.diff(resource.data).affectedKeys()
                   .hasOnly(['unlockedAt'])
                && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /farpizza-referrals/{referralId} {
  allow read: if true;
  allow create: if isValidAddress(request.resource.data.newUserAddress)
                && isValidUsername(request.resource.data.referrerUsername)
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}


// ── LEGACY (read-only or locked) ──

match /crew-chat/{msgId} {
  allow read: if true;
  allow create: if request.resource.data.fid is int
                && request.resource.data.fid > 0
                && isValidString(request.resource.data.text, 1, 280)
                && request.resource.data.size() <= 10000;
  allow update, delete: if false;
}

match /bakery-history/{entryId} {
  allow read: if true;
  allow create: if request.resource.data.size() <= 50000;
  allow update, delete: if false;
}

match /farcots-scores/{scoreId} {
  allow read: if true;
  allow create, update: if isValidAddress(request.resource.data.address)
                        && request.resource.data.size() <= 50000;
  allow delete: if false;
}

match /leaderboard/{entryId} { allow read: if true; allow write: if false; }
match /messages/{msgId} { allow read: if true; allow write: if false; }
match /pit-scores/{scoreId} { allow read: if true; allow write: if false; }
match /recipe-votes/{voteId} { allow read: if true; allow write: if false; }
match /schedule-requests/{requestId} { allow read: if true; allow write: if false; }
match /sprinkles-messages/{msgId} { allow read: if true; allow write: if false; }
match /tags/{tagId} { allow read: if true; allow write: if false; }
match /tosses/{tossId} { allow read: if true; allow write: if false; }
match /track-votes/{voteId} { allow read: if true; allow write: if false; }
match /visits/{visitId} { allow read: if true; allow write: if false; }
match /crew-mine-messages/{msgId} { allow read: if true; allow write: if false; }
